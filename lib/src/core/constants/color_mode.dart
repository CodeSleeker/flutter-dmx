/// Indicates what type of color channels a fixture uses.
enum ColorMode {
  /// Standard RGB (Red, Green, Blue)
  rgb,

  /// RGB plus an additional White channel
  rgbw,

  /// White-only fixture
  white,
}
